"""Desktop user interface (Phase 8), built on PySide6.

Import nothing heavy here: the CLI imports this package's entry point
lazily, so a missing or broken Qt installation must not take the console
commands down with it.
"""
